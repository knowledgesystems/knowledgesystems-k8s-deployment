master:
  extraFlags:
  - --maxmemory 8gb
  - --maxmemory-policy volatile-ttl
  nodeSelector:
    kops.k8s.io/instancegroup: redis-persistence-cache
  persistence:
    size: 64Gi
slave:
  extraFlags:
  - --maxmemory 8gb
  - --maxmemory-policy volatile-ttl
  persistence:
    size: 64Gi
