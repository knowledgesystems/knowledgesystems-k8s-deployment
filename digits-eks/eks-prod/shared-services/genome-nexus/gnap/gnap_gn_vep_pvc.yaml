apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gnap-gn-vep-pvc
  namespace: genome-nexus
spec:
  accessModes:
    - ReadWriteOnce #Access modes for volumes is defined under Persistent Volumes
  resources:
    requests:
      storage: 60Gi # volume size requested
