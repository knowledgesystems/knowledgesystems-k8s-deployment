apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
  namespace: traefik
spec:
  secretName: tls-http-echo
  dnsNames:
    - "argocd.oncokb.org"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer