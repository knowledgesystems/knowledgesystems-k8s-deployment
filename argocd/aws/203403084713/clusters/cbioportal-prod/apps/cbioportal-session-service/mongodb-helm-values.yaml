image:
  tag: "4.2"
nodeSelector:
  workload: cbio-session
persistence:
  size: 200Gi
  resourcePolicy: "keep"
tolerations:
  - effect: NoSchedule
    key: workload
    operator: Equal
    value: cbio-session
auth:
  existingSecret: cbioportal-session-service-mongodb-creds