apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vibe-vep-data-pvc
  namespace: genome-nexus
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 50Gi
