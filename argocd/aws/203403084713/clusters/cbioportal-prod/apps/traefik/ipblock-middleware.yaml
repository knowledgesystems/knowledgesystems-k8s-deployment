apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ipblock
  namespace: default
spec:
  plugin:
    ipblock:
      ipDenyList:
        - 14.139.32.2
        # Huawei-Cloud-SG (PetalBot). Fetches robots.txt but ignores it; ~1,600/hr
        # patient-page render fan-out. Datacenter range, no real users.
        - 114.119.128.0/19
