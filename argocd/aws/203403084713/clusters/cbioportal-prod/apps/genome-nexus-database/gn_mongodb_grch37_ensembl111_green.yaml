apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gn-mongo-grch37-ensembl111-green
  namespace: genome-nexus
  labels:
    app: gn-mongo-grch37-ensembl111-green
spec:
  serviceName: gn-mongo-grch37-ensembl111-green
  replicas: 1
  selector:
    matchLabels:
      app: gn-mongo-grch37-ensembl111-green
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gn-mongo-grch37-ensembl111-green
    spec:
      containers:
        - name: mongodb
          image: mongo:5.0
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              name: mongodb
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data/db
              subPath: data/db
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: gn-mongodb-grch37-ensembl111-v1dot2-pvc
      tolerations:
        - key: workload
          operator: Equal
          value: gn-database
          effect: NoSchedule
      nodeSelector:
        workload: gn-database
        topology.kubernetes.io/zone: us-east-1b # This should match the availability zone of the underlying volumes created in terraform
---
apiVersion: v1
kind: Service
metadata:
  name: gn-mongo-grch37-ensembl111-green
  namespace: genome-nexus
  labels:
    app: gn-mongo-grch37-ensembl111-green
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongodb
  selector:
    app: gn-mongo-grch37-ensembl111-green
