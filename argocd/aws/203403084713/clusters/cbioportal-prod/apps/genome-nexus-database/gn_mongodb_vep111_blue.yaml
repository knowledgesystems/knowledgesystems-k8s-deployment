apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gn-mongo-vep111-blue
  namespace: genome-nexus
  labels:
    app: gn-mongo-vep111-blue
spec:
  serviceName: gn-mongo-vep111-blue
  replicas: 1
  selector:
    matchLabels:
      app: gn-mongo-vep111-blue
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gn-mongo-vep111-blue
    spec:
      containers:
        - name: mongodb
          image: genomenexus/gn-mongo:1.3-mutationassessor
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              name: mongodb
              protocol: TCP
          env:
            - name: COLLECTION_UPDATE_LIST
              value: "clinvar.mutation,ensembl.biomart_transcripts,ensembl.canonical_transcript_per_hgnc,hotspot.mutation,signal.mutation,ensembl.nucleotide_context,oncokb.gene,version"
            - name: MONGO_DB
              value: "annotator"
          volumeMounts:
            - name: data
              mountPath: /data/db
              subPath: data/db    # Need this for Bitnami snapshot data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: gn-mongodb-vep111-pvc
      tolerations:
        - key: workload
          operator: Equal
          value: gn-db-sm
          effect: NoSchedule
      nodeSelector:
        workload: gn-db-sm
        topology.kubernetes.io/zone: us-east-1b
---
apiVersion: v1
kind: Service
metadata:
  name: gn-mongo-vep111-blue
  namespace: genome-nexus
  labels:
    app: gn-mongo-vep111-blue
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongodb
  selector:
    app: gn-mongo-vep111-blue
