apiVersion: v1
kind: PersistentVolume
metadata:
  name: gn-mongodb-genie-pv
  namespace: genome-nexus
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: ebs.csi.aws.com
    volumeHandle: vol-0ef938584c767e491
    fsType: ext4
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: topology.kubernetes.io/zone
              operator: In
              values:
                - us-east-1b
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gn-mongodb-genie-pvc
  namespace: genome-nexus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: gn-mongodb-genie-pv
