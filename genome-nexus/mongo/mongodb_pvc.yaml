apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: genome-nexus-mongodump-pvc
  namespace: genome-nexus
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi

